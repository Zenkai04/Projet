<!-- filepath: /u:/Mes documents/Projet/templates/restaurants.twig -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurants</title>
</head>
<body>
    <h1>Restaurants</h1>
    {% if restaurants is not empty %}
        <ul>
            {% for restaurant in restaurants %}
                <li>
                    <strong>{{ restaurant.name }}</strong>
                    <a href="commentaire.php?restaurant_id={{ restaurant.id }}">View Comments</a>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>No restaurants available.</p>
    {% endif %}
</body>
</html>