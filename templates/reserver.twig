<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réserver</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="icon" type="image/png" href="../public/images/logo.ico">
</head>
<body>
    <div class="container mt-5">
        <h2>Réserver une table chez {{ restaurant.name }}</h2>
        <form method="post" action="?page=reserver">
            <input type="hidden" name="idRestaurant" value="{{ restaurant.id }}">
            <div class="form-group">
                <label for="nbPersonnes">Nombre de personnes</label>
                <input type="number" class="form-control" id="nbPersonnes" name="nbPersonnes" required>
            </div>
            <div class="form-group">
                <label for="dateHeure">Date et Heure</label>
                <input type="datetime-local" class="form-control" id="dateHeure" name="dateHeure" required>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">Réserver</button>
                <a href="?page=details&id={{ restaurant.id }}" class="btn btn-secondary">Annuler</a>
            </div>
        </form>
    </div>
</body>
</html>